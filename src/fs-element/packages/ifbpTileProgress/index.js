import IfbpTileProgress from './src/ifbpTileProgress';

/* istanbul ignore next */
IfbpTileProgress.install = function(Vue) {
  Vue.component(IfbpTileProgress.name, IfbpTileProgress);
};

export default IfbpTileProgress;
