import IfbpDelDialog from './src/ifbp-del-dialog';

/* istanbul ignore next */
IfbpDelDialog.install = function(Vue) {
  Vue.component(IfbpDelDialog.name, IfbpDelDialog);
};

export default IfbpDelDialog;
